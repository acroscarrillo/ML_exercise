Neural networks from complete scratch in Julia (no libraries!). Implemented with an elegant `struct` and with GPU capability via the `Metal.jl` library (Apple's equivalent `CUDA` but for M1/M2/M3 architecture). 
